# 产品形态


## 本地 gui agent
部署在本地并控制整机的 gui agent,

在熄屏后将会被迫终止。

## C/S 架构 gui agent 
被控机器运行 server, 主控机器运行 client/ gui agent

被控机器可以通过 server，获取无障碍 API。

## 双机位 gui agent
采集卡，硬件级隔离，被控机器完全无感，主控机器获得全量屏幕，被控机器完全无感，